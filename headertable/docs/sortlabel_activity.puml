@startuml
title Activity Diagram - sortlabel.go
start
:Load headertable/sortlabel.go;
:1. NewSortingLabel;
:2. SetState [method on *sortingLabel];
if (branch or error path?) then (yes)
:Handle alternative/error path;
else (no)
:Continue primary flow;
endif
:3. nextState [method on *sortingLabel];
if (branch or error path?) then (yes)
:Handle alternative/error path;
else (no)
:Continue primary flow;
endif
:4. OnTapped [method on *sortingLabel];
if (branch or error path?) then (yes)
:Handle alternative/error path;
else (no)
:Continue primary flow;
endif
:5. CreateRenderer [method on *sortingLabel];
:6. MinSize [method on *sortingLabelRenderer];
:7. Layout [method on *sortingLabelRenderer];
:8. Refresh [method on *sortingLabelRenderer];
:9. Objects [method on *sortingLabelRenderer];
:10. Destroy [method on *sortingLabelRenderer];
stop
@enduml
