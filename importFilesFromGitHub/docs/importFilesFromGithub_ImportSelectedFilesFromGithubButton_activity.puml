@startuml
title Activity Diagram - importFilesFromGithub_ImportSelectedFilesFromGithubButton.go
start
:Load importFilesFromGitHub/importFilesFromGithub_ImportSelectedFilesFromGithubButton.go;
:1. decodeBase64Content [method on *ImportFilesFromGitHubStruct];
if (branch or error path?) then (yes)
:Handle alternative/error path;
else (no)
:Continue primary flow;
endif
:2. extractContentFromJson [method on *ImportFilesFromGitHubStruct];
if (branch or error path?) then (yes)
:Handle alternative/error path;
else (no)
:Continue primary flow;
endif
:3. generateImportSelectedFilesFromGithubButton [method on *ImportFilesFromGitHubStruct];
if (branch or error path?) then (yes)
:Handle alternative/error path;
else (no)
:Continue primary flow;
endif
:Iterate loop/range logic;
stop
@enduml
