@startuml
title Activity Diagram - importFilesFromGithub_SelectedFilesListTable.go
start
:Load importFilesFromGitHub/importFilesFromGithub_SelectedFilesListTable.go;
:1. newClickableLabel [method on *ImportFilesFromGitHubStruct];
:2. Tapped [method on *clickableLabel];
if (branch or error path?) then (yes)
:Handle alternative/error path;
else (no)
:Continue primary flow;
endif
:3. TappedSecondary [method on *clickableLabel];
:4. MouseIn [method on *clickableLabel];
if (branch or error path?) then (yes)
:Handle alternative/error path;
else (no)
:Continue primary flow;
endif
:5. MouseMoved [method on *clickableLabel];
:6. MouseOut [method on *clickableLabel];
if (branch or error path?) then (yes)
:Handle alternative/error path;
else (no)
:Continue primary flow;
endif
:7. generateSelectedFilesListTable [method on *ImportFilesFromGitHubStruct];
:8. UpdateSelectedFilesTable [method on *ImportFilesFromGitHubStruct];
if (branch or error path?) then (yes)
:Handle alternative/error path;
else (no)
:Continue primary flow;
endif
:Iterate loop/range logic;
:9. newCustomLabel [method on *ImportFilesFromGitHubStruct];
:10. Tapped [method on *customLabel];
if (branch or error path?) then (yes)
:Handle alternative/error path;
else (no)
:Continue primary flow;
endif
:11. TappedSecondary [method on *customLabel];
:12. MouseIn [method on *customLabel];
stop
@enduml
