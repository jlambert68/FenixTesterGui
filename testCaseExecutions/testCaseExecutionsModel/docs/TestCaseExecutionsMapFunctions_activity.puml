@startuml
title Activity Diagram - TestCaseExecutionsMapFunctions.go
start
:Load testCaseExecutions/testCaseExecutionsModel/TestCaseExecutionsMapFunctions.go;
:1. InitiateTestCaseExecutionsMap [method on TestCaseExecutionsModelStruct];
if (branch or error path?) then (yes)
:Handle alternative/error path;
else (no)
:Continue primary flow;
endif
:Schedule deferred cleanup;
:2. ReadFromTestCaseExecutionsMap [method on TestCaseExecutionsModelStruct];
if (branch or error path?) then (yes)
:Handle alternative/error path;
else (no)
:Continue primary flow;
endif
:Schedule deferred cleanup;
:3. ReadAllFromTestCaseExecutionsMap [method on TestCaseExecutionsModelStruct];
if (branch or error path?) then (yes)
:Handle alternative/error path;
else (no)
:Continue primary flow;
endif
:Iterate loop/range logic;
:Schedule deferred cleanup;
:4. GetNumberOfTestCaseExecutionsRetrievedFromDatabase [method on TestCaseExecutionsModelStruct];
:Schedule deferred cleanup;
:5. AddToTestCaseExecutionsMap [method on TestCaseExecutionsModelStruct];
if (branch or error path?) then (yes)
:Handle alternative/error path;
else (no)
:Continue primary flow;
endif
:Schedule deferred cleanup;
:6. DeleteFromTestCaseExecutionsMap [method on TestCaseExecutionsModelStruct];
if (branch or error path?) then (yes)
:Handle alternative/error path;
else (no)
:Continue primary flow;
endif
:Schedule deferred cleanup;
:7. GetTestInstructionExecutionUuidFromTestInstructionUuid [method on TestCaseExecutionsModelStruct];
if (branch or error path?) then (yes)
:Handle alternative/error path;
else (no)
:Continue primary flow;
endif
:Schedule deferred cleanup;
:8. GetTestInstructionFromTestInstructionExecutionUuid [method on TestCaseExecutionsModelStruct];
if (branch or error path?) then (yes)
:Handle alternative/error path;
else (no)
:Continue primary flow;
endif
:Schedule deferred cleanup;
stop
@enduml
