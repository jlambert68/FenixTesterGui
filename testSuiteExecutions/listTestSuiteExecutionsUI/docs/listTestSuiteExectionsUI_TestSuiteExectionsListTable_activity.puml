@startuml
title Activity Diagram - listTestSuiteExectionsUI_TestSuiteExectionsListTable.go
start
:Load testSuiteExecutions/listTestSuiteExecutionsUI/listTestSuiteExectionsUI_TestSuiteExectionsListTable.go;
:1. RemoveTestSuiteExecutionFromList;
:2. SortGuiTableOnCurrentColumnAndSorting;
if (branch or error path?) then (yes)
:Handle alternative/error path;
else (no)
:Continue primary flow;
endif
:3. SortOrReverseSortGuiTable;
if (branch or error path?) then (yes)
:Handle alternative/error path;
else (no)
:Continue primary flow;
endif
:4. calculateAndSetCorrectColumnWidths;
if (branch or error path?) then (yes)
:Handle alternative/error path;
else (no)
:Continue primary flow;
endif
:Iterate loop/range logic;
:5. generateTestSuiteExecutionsListTable;
if (branch or error path?) then (yes)
:Handle alternative/error path;
else (no)
:Continue primary flow;
endif
:6. loadTestSuiteExecutionListTableTable;
if (branch or error path?) then (yes)
:Handle alternative/error path;
else (no)
:Continue primary flow;
endif
:Iterate loop/range logic;
:Schedule deferred cleanup;
:7. sort2DStringSlice;
if (branch or error path?) then (yes)
:Handle alternative/error path;
else (no)
:Continue primary flow;
endif
:8. sortGuiTableAscendingOnTestSuiteExecutionTimeStamp;
:9. sortGuiTableOnColumn;
if (branch or error path?) then (yes)
:Handle alternative/error path;
else (no)
:Continue primary flow;
endif
:Iterate loop/range logic;
:10. updateTestSuiteExecutionsListTable;
if (branch or error path?) then (yes)
:Handle alternative/error path;
else (no)
:Continue primary flow;
endif
:Schedule deferred cleanup;
stop
@enduml
