@startuml
title Activity Diagram - detailedTestSuiteExecutionsMapFunctions.go
start
:Load testSuiteExecutions/testSuiteExecutionsModel/detailedTestSuiteExecutionsMapFunctions.go;
:1. ReadFromDetailedTestSuiteExecutionsMap [method on TestSuiteExecutionsModelStruct];
if (branch or error path?) then (yes)
:Handle alternative/error path;
else (no)
:Continue primary flow;
endif
:Schedule deferred cleanup;
:2. GetNumberOfDetailedTestSuiteExecutionsRetrievedFromDatabase [method on TestSuiteExecutionsModelStruct];
if (branch or error path?) then (yes)
:Handle alternative/error path;
else (no)
:Continue primary flow;
endif
:Schedule deferred cleanup;
:3. AddToDetailedTestSuiteExecutionsMap [method on TestSuiteExecutionsModelStruct];
if (branch or error path?) then (yes)
:Handle alternative/error path;
else (no)
:Continue primary flow;
endif
:Schedule deferred cleanup;
:4. DeleteFromDetailedTestSuiteExecutionsMap [method on TestSuiteExecutionsModelStruct];
if (branch or error path?) then (yes)
:Handle alternative/error path;
else (no)
:Continue primary flow;
endif
:Schedule deferred cleanup;
:5. SetFlagRefreshOngoingOfDetailedTestSuiteExecution [method on TestSuiteExecutionsModelStruct];
if (branch or error path?) then (yes)
:Handle alternative/error path;
else (no)
:Continue primary flow;
endif
:Schedule deferred cleanup;
:6. ClearFlagRefreshOngoingOfDetailedTestSuiteExecution [method on TestSuiteExecutionsModelStruct];
if (branch or error path?) then (yes)
:Handle alternative/error path;
else (no)
:Continue primary flow;
endif
:Schedule deferred cleanup;
stop
@enduml
