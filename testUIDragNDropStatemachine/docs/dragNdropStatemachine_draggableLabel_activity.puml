@startuml
title Activity Diagram - dragNdropStatemachine_draggableLabel.go
start
:Load testUIDragNDropStatemachine/dragNdropStatemachine_draggableLabel.go;
:1. NewDraggableLabel [method on *StateMachineDragAndDropStruct];
:2. Dragged [method on *DraggableLabel];
if (branch or error path?) then (yes)
:Handle alternative/error path;
else (no)
:Continue primary flow;
endif
:3. DragEnd [method on *DraggableLabel];
if (branch or error path?) then (yes)
:Handle alternative/error path;
else (no)
:Continue primary flow;
endif
:Iterate loop/range logic;
:4. MouseIn [method on *DraggableLabel];
if (branch or error path?) then (yes)
:Handle alternative/error path;
else (no)
:Continue primary flow;
endif
:5. MouseMoved [method on *DraggableLabel];
:6. MouseOut [method on *DraggableLabel];
if (branch or error path?) then (yes)
:Handle alternative/error path;
else (no)
:Continue primary flow;
endif
stop
@enduml
